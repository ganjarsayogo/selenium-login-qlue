in order to run this you need to install:

1. python (don't forget to check ADD PATH in dialog prompt)
2. open cmd
3. type "pip install selenium"
4. type "pip install webdriver-manager"
5. run the qlue_login.py with command "python -m unittest -v -b qlue_login.py"